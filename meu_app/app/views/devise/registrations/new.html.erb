<!DOCTYPE html>
<html>
  <head>
    <title>MeuApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-r from-pink-100 via-purple-100 to-pink-200 min-h-screen flex items-center justify-center">

    <div class="w-full max-w-3xl bg-white shadow-2xl rounded-3xl border border-pink-200 p-12 mx-4">
      <h2 class="text-4xl font-bold mb-8 text-center text-pink-600">Crie sua conta ✨</h2>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="mb-6">
          <%= f.label :email, class: "block text-pink-700 font-medium mb-2" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", 
              class: "w-full px-5 py-3 border border-pink-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-pink-300 transition" %>
        </div>

        <div class="mb-6">
          <%= f.label :password, class: "block text-pink-700 font-medium mb-2" %>
          <% if @minimum_password_length %>
            <p class="text-sm text-pink-400 mb-1">(<%= @minimum_password_length %> caracteres mínimo)</p>
          <% end %>
          <%= f.password_field :password, autocomplete: "new-password", 
              class: "w-full px-5 py-3 border border-pink-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-pink-300 transition" %>
        </div>

        <div class="mb-6">
          <%= f.label :password_confirmation, class: "block text-pink-700 font-medium mb-2" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", 
              class: "w-full px-5 py-3 border border-pink-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-pink-300 transition" %>
        </div>

        <div class="mb-6">
          <%= f.submit "Cadastrar", class: "w-full bg-pink-500 text-white py-3 rounded-xl hover:bg-pink-600 hover:shadow-lg transition duration-200" %>
        </div>
      <% end %>

      <div class="text-center text-sm text-pink-500">
        <%= render "devise/shared/links" %>
      </div>
    </div>

  </body>
</html>
