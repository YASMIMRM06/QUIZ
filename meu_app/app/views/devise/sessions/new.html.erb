<!DOCTYPE html>
<html>
  <head>
    <title>MeuApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-r from-pink-100 via-purple-100 to-pink-200 min-h-screen flex items-center justify-center">

    <div class="w-full max-w-3xl bg-white shadow-2xl rounded-3xl border border-pink-200 p-12 mx-4">
      <h2 class="text-4xl font-extrabold text-center text-pink-600 mb-10 tracking-wide drop-shadow-sm">
        Bem-vinda ao Sistema ✨
      </h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="mb-6">
          <%= f.label :email, class: "block text-pink-700 font-semibold mb-2 text-lg" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email",
              class: "w-full px-5 py-3 text-lg border border-pink-300 rounded-xl focus:ring-4 focus:ring-pink-300 focus:outline-none transition" %>
        </div>

        <div class="mb-6">
          <%= f.label :password, class: "block text-pink-700 font-semibold mb-2 text-lg" %>
          <%= f.password_field :password, autocomplete: "current-password",
              class: "w-full px-5 py-3 text-lg border border-pink-300 rounded-xl focus:ring-4 focus:ring-pink-300 focus:outline-none transition" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="mb-6 flex items-center text-lg">
            <%= f.check_box :remember_me, class: "mr-3 w-5 h-5 accent-pink-500" %>
            <%= f.label :remember_me, class: "text-gray-700" %>
          </div>
        <% end %>

        <div class="mb-8">
          <%= f.submit "Entrar no Sistema", 
              class: "w-full py-3 text-xl bg-pink-500 text-white font-bold tracking-wide rounded-xl
                      hover:bg-pink-600 hover:shadow-2xl transform hover:scale-[1.03] transition duration-200" %>
        </div>
      <% end %>

      <div class="text-center text-lg text-pink-600">
        <%= render "devise/shared/links" %>
      </div>
    </div>

  </body>
</html>
