<!DOCTYPE html>
<html>
  <head>
    <title>MeuApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-r from-pink-100 via-purple-100 to-pink-200 min-h-screen flex flex-col">

    <!-- Navbar -->
    <% if user_signed_in? %>
      <nav class="bg-white border-b border-pink-200 shadow-lg">
        <div class="container mx-auto px-6 py-4 flex items-center justify-between">

          <!-- Menu à esquerda -->
          <ul class="flex items-center space-x-8 text-pink-700 font-semibold">
            <li>
              <%= link_to "Home", root_path, class: "hover:text-pink-500 transition duration-200" %>
            </li>
            <!-- Adicione outros links aqui, ex: Questionários, Perfil -->
          </ul>

          <!-- Área do usuário à direita -->
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <span class="text-sm text-pink-600 font-medium">
                👤 <%= current_user.email %>
              </span>

              <%= link_to "Sair", destroy_user_session_path,
                method: :delete,
                data: { turbo_method: :delete },
                class: "bg-pink-500 text-white px-4 py-2 rounded-xl hover:bg-pink-600 shadow-md transition duration-200 font-bold"
              %>
            <% end %>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Conteúdo principal centralizado -->
    <main class="flex flex-1 flex-col items-center justify-center px-5 w-full">
      <%= yield %>
    </main>

  </body>
</html>
