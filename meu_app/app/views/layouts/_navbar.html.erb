<nav class="bg-white border-b border-pink-200 shadow-lg">
  <div class="container mx-auto px-6 py-4 flex items-center justify-between">

    <!-- Menu à esquerda -->
    <ul class="flex items-center space-x-8 text-pink-700 font-semibold">
      <li>
        <%= link_to "Home", root_path, class: "hover:text-pink-500 transition duration-200" %>
      </li>
    </ul>

    <!-- Área do usuário à direita -->
    <div class="flex items-center space-x-4">
      <% if user_signed_in? %>
        <span class="text-sm text-pink-600 font-medium">
          👤 <%= current_user.email %>
        </span>

        <%= link_to "Sair", destroy_user_session_path,
          method: :delete,
          data: { turbo_method: :delete },
          class: "bg-pink-500 text-white px-4 py-2 rounded-xl hover:bg-pink-600 shadow-md transition duration-200 font-bold"
        %>
      <% end %>
    </div>
  </div>
</nav>
